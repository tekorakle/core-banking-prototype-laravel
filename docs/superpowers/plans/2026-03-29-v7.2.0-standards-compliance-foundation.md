# v7.2.0 Standards & Compliance Foundation — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add ISO 20022 message engine, Open Banking PSD2 compliance layer, and ISO 8583 card network processor to close FinAegis's traditional banking infrastructure gaps.

**Architecture:** Three new domains (`ISO20022`, `OpenBanking`, `ISO8583`) following existing DDD patterns. Each domain has Services, ValueObjects, Enums, Models (where applicable), Routes, and GraphQL schemas. All domains are opt-in via config flags. ISO 20022 serves as internal message format for payment rails in future phases.

**Tech Stack:** PHP 8.4, Laravel 12, Pest, PHPStan Level 8, Lighthouse GraphQL, Sanctum auth

**Spec:** `docs/superpowers/specs/2026-03-29-standards-compliance-gap-closure-design.md` — Phase 1

---

## File Structure

### ISO 20022 Domain
```
app/Domain/ISO20022/
├── Enums/
│   ├── MessageFamily.php
│   └── TransactionStatus.php
├── Services/
│   ├── MessageParser.php
│   ├── MessageGenerator.php
│   ├── MessageValidator.php
│   └── MessageRegistry.php
└── ValueObjects/
    ├── BusinessApplicationHeader.php
    ├── Pain001.php
    ├── Pain008.php
    ├── Pacs002.php
    ├── Pacs003.php
    ├── Pacs004.php
    ├── Pacs008.php
    ├── Camt053.php
    └── Camt054.php

config/iso20022.php
graphql/iso20022.graphql
app/GraphQL/Queries/ISO20022/ValidateMessageQuery.php
app/GraphQL/Queries/ISO20022/MessageStatusQuery.php
app/Domain/ISO20022/Routes/api.php

tests/Unit/Domain/ISO20022/
├── Enums/MessageFamilyTest.php
├── Services/MessageParserTest.php
├── Services/MessageGeneratorTest.php
├── Services/MessageValidatorTest.php
└── ValueObjects/Pain001Test.php
```

### Open Banking Domain
```
app/Domain/OpenBanking/
├── Enums/
│   ├── ConsentStatus.php
│   ├── ConsentPermission.php
│   └── OpenBankingStandard.php
├── Models/
│   ├── Consent.php
│   ├── TppRegistration.php
│   └── ConsentAccessLog.php
├── Services/
│   ├── ConsentService.php
│   ├── ConsentEnforcementService.php
│   ├── AispService.php
│   ├── PispService.php
│   ├── TppRegistrationService.php
│   ├── BerlinGroupAdapter.php
│   └── UkObAdapter.php
└── Routes/
    └── api.php

app/Http/Middleware/ValidateTppCertificate.php
app/Http/Middleware/EnforceConsent.php
config/openbanking.php
graphql/open-banking.graphql
app/GraphQL/Queries/OpenBanking/ConsentQuery.php
app/GraphQL/Mutations/OpenBanking/CreateConsentMutation.php
app/GraphQL/Mutations/OpenBanking/RevokeConsentMutation.php

database/migrations/2026_03_29_000001_create_consents_table.php
database/migrations/2026_03_29_000002_create_tpp_registrations_table.php
database/migrations/2026_03_29_000003_create_consent_access_logs_table.php

tests/Unit/Domain/OpenBanking/
├── Enums/ConsentStatusTest.php
├── Services/ConsentServiceTest.php
├── Services/ConsentEnforcementServiceTest.php
├── Services/AispServiceTest.php
├── Services/PispServiceTest.php
├── Services/BerlinGroupAdapterTest.php
└── Services/UkObAdapterTest.php
tests/Feature/Api/OpenBanking/ConsentFlowTest.php
```

### ISO 8583 Domain
```
app/Domain/ISO8583/
├── Enums/
│   ├── MessageTypeIndicator.php
│   ├── ProcessingCode.php
│   └── ResponseCode.php
├── Services/
│   ├── MessageCodec.php
│   ├── FieldDefinitions.php
│   ├── AuthorizationHandler.php
│   ├── ReversalHandler.php
│   └── SettlementHandler.php
└── ValueObjects/
    ├── Iso8583Message.php
    ├── DataElement.php
    └── Bitmap.php

config/iso8583.php
app/Domain/ISO8583/Routes/api.php

tests/Unit/Domain/ISO8583/
├── Enums/MessageTypeIndicatorTest.php
├── Services/MessageCodecTest.php
├── Services/AuthorizationHandlerTest.php
└── ValueObjects/BitmapTest.php
```

---

## Task 1: ISO 20022 — Config, Enums, and Business Application Header

**Files:**
- Create: `config/iso20022.php`
- Create: `app/Domain/ISO20022/Enums/MessageFamily.php`
- Create: `app/Domain/ISO20022/Enums/TransactionStatus.php`
- Create: `app/Domain/ISO20022/ValueObjects/BusinessApplicationHeader.php`
- Test: `tests/Unit/Domain/ISO20022/Enums/MessageFamilyTest.php`

- [ ] **Step 1: Create config file**

```php
<?php
// config/iso20022.php
declare(strict_types=1);

return [
    'enabled' => env('ISO20022_ENABLED', false),
    'enabled_families' => array_filter(explode(',', (string) env('ISO20022_FAMILIES', 'pain,pacs,camt'))),
    'validation_strictness' => env('ISO20022_STRICT', true),
    'schema_path' => storage_path('app/iso20022/schemas'),
    'default_currency' => env('ISO20022_DEFAULT_CURRENCY', 'EUR'),
    'uetr_enabled' => (bool) env('ISO20022_UETR_ENABLED', true),
    'max_message_size_kb' => (int) env('ISO20022_MAX_SIZE_KB', 512),
    'supported_versions' => [
        'pain.001' => '001.001.09',
        'pain.008' => '008.001.08',
        'pacs.008' => '008.001.08',
        'pacs.002' => '002.001.10',
        'pacs.003' => '003.001.08',
        'pacs.004' => '004.001.09',
        'camt.053' => '053.001.08',
        'camt.054' => '054.001.08',
    ],
];
```

- [ ] **Step 2: Create MessageFamily enum**

```php
<?php
// app/Domain/ISO20022/Enums/MessageFamily.php
declare(strict_types=1);

namespace App\Domain\ISO20022\Enums;

enum MessageFamily: string
{
    case PAIN = 'pain';
    case PACS = 'pacs';
    case CAMT = 'camt';
    case ACMT = 'acmt';
    case SEMT = 'semt';

    public function label(): string
    {
        return match ($this) {
            self::PAIN => 'Payment Initiation',
            self::PACS => 'Payments Clearing and Settlement',
            self::CAMT => 'Cash Management',
            self::ACMT => 'Account Management',
            self::SEMT => 'Securities Management',
        };
    }
}
```

- [ ] **Step 3: Create TransactionStatus enum**

```php
<?php
// app/Domain/ISO20022/Enums/TransactionStatus.php
declare(strict_types=1);

namespace App\Domain\ISO20022\Enums;

enum TransactionStatus: string
{
    case ACCP = 'ACCP'; // AcceptedCustomerProfile
    case ACSP = 'ACSP'; // AcceptedSettlementInProcess
    case ACSC = 'ACSC'; // AcceptedSettlementCompleted
    case ACTC = 'ACTC'; // AcceptedTechnicalValidation
    case ACWC = 'ACWC'; // AcceptedWithChange
    case PDNG = 'PDNG'; // Pending
    case RCVD = 'RCVD'; // Received
    case RJCT = 'RJCT'; // Rejected
    case CANC = 'CANC'; // Cancelled

    public function isTerminal(): bool
    {
        return in_array($this, [self::ACSC, self::RJCT, self::CANC], true);
    }

    public function isSuccessful(): bool
    {
        return in_array($this, [self::ACSC, self::ACSP, self::ACCP], true);
    }
}
```

- [ ] **Step 4: Create BusinessApplicationHeader VO**

```php
<?php
// app/Domain/ISO20022/ValueObjects/BusinessApplicationHeader.php
declare(strict_types=1);

namespace App\Domain\ISO20022\ValueObjects;

use App\Domain\ISO20022\Enums\MessageFamily;
use DateTimeImmutable;
use Illuminate\Support\Str;

final readonly class BusinessApplicationHeader
{
    public function __construct(
        public string $businessMessageId,
        public string $messageDefinitionId,
        public string $from,
        public string $to,
        public DateTimeImmutable $creationDate,
        public ?string $uetr = null,
    ) {
    }

    public static function create(
        string $messageDefinitionId,
        string $from,
        string $to,
        ?string $uetr = null,
    ): self {
        return new self(
            businessMessageId: Str::uuid()->toString(),
            messageDefinitionId: $messageDefinitionId,
            from: $from,
            to: $to,
            creationDate: new DateTimeImmutable(),
            uetr: $uetr ?? (config('iso20022.uetr_enabled') ? Str::uuid()->toString() : null),
        );
    }

    public function family(): MessageFamily
    {
        $prefix = explode('.', $this->messageDefinitionId)[0] ?? '';

        return MessageFamily::from($prefix);
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        return [
            'business_message_id' => $this->businessMessageId,
            'message_definition_id' => $this->messageDefinitionId,
            'from' => $this->from,
            'to' => $this->to,
            'creation_date' => $this->creationDate->format('Y-m-d\TH:i:s.vP'),
            'uetr' => $this->uetr,
        ];
    }
}
```

- [ ] **Step 5: Write tests**

```php
<?php
// tests/Unit/Domain/ISO20022/Enums/MessageFamilyTest.php
declare(strict_types=1);

use App\Domain\ISO20022\Enums\MessageFamily;
use App\Domain\ISO20022\Enums\TransactionStatus;
use App\Domain\ISO20022\ValueObjects\BusinessApplicationHeader;

it('has correct message family values', function (): void {
    expect(MessageFamily::PAIN->value)->toBe('pain');
    expect(MessageFamily::PACS->value)->toBe('pacs');
    expect(MessageFamily::CAMT->value)->toBe('camt');
});

it('returns human-readable labels', function (): void {
    expect(MessageFamily::PAIN->label())->toBe('Payment Initiation');
    expect(MessageFamily::PACS->label())->toBe('Payments Clearing and Settlement');
});

it('identifies terminal transaction statuses', function (): void {
    expect(TransactionStatus::ACSC->isTerminal())->toBeTrue();
    expect(TransactionStatus::RJCT->isTerminal())->toBeTrue();
    expect(TransactionStatus::CANC->isTerminal())->toBeTrue();
    expect(TransactionStatus::PDNG->isTerminal())->toBeFalse();
    expect(TransactionStatus::ACSP->isTerminal())->toBeFalse();
});

it('identifies successful transaction statuses', function (): void {
    expect(TransactionStatus::ACSC->isSuccessful())->toBeTrue();
    expect(TransactionStatus::RJCT->isSuccessful())->toBeFalse();
});

it('creates business application header with UETR', function (): void {
    $bah = BusinessApplicationHeader::create(
        messageDefinitionId: 'pacs.008.001.08',
        from: 'BANKUS33',
        to: 'BANKGB2L',
    );

    expect($bah->businessMessageId)->not->toBeEmpty();
    expect($bah->messageDefinitionId)->toBe('pacs.008.001.08');
    expect($bah->from)->toBe('BANKUS33');
    expect($bah->to)->toBe('BANKGB2L');
    expect($bah->family())->toBe(MessageFamily::PACS);
});

it('converts header to array', function (): void {
    $bah = BusinessApplicationHeader::create(
        messageDefinitionId: 'pain.001.001.09',
        from: 'SENDER',
        to: 'RECEIVER',
    );

    $array = $bah->toArray();
    expect($array)->toHaveKeys([
        'business_message_id',
        'message_definition_id',
        'from',
        'to',
        'creation_date',
        'uetr',
    ]);
});
```

- [ ] **Step 6: Run tests**

Run: `XDEBUG_MODE=off vendor/bin/pest tests/Unit/Domain/ISO20022/Enums/MessageFamilyTest.php`
Expected: All 6 tests pass

- [ ] **Step 7: PHPStan check**

Run: `XDEBUG_MODE=off vendor/bin/phpstan analyse --memory-limit=2G app/Domain/ISO20022/ config/iso20022.php`
Expected: No errors

- [ ] **Step 8: Commit**

```bash
git add app/Domain/ISO20022/Enums/ app/Domain/ISO20022/ValueObjects/BusinessApplicationHeader.php config/iso20022.php tests/Unit/Domain/ISO20022/
git commit -m "feat(iso20022): domain scaffolding — config, enums, BAH value object"
```

---

## Task 2: ISO 20022 — Message DTOs

**Files:**
- Create: `app/Domain/ISO20022/ValueObjects/Pain001.php`
- Create: `app/Domain/ISO20022/ValueObjects/Pain008.php`
- Create: `app/Domain/ISO20022/ValueObjects/Pacs008.php`
- Create: `app/Domain/ISO20022/ValueObjects/Pacs002.php`
- Create: `app/Domain/ISO20022/ValueObjects/Pacs003.php`
- Create: `app/Domain/ISO20022/ValueObjects/Pacs004.php`
- Create: `app/Domain/ISO20022/ValueObjects/Camt053.php`
- Create: `app/Domain/ISO20022/ValueObjects/Camt054.php`
- Test: `tests/Unit/Domain/ISO20022/ValueObjects/Pain001Test.php`

Each DTO is a readonly class representing one ISO 20022 message type. All share the pattern: constructor with typed properties, `toArray()`, `toXml()`, and `static fromArray()`.

- [ ] **Step 1: Create Pain001 (Customer Credit Transfer Initiation)**

```php
<?php
// app/Domain/ISO20022/ValueObjects/Pain001.php
declare(strict_types=1);

namespace App\Domain\ISO20022\ValueObjects;

use DateTimeImmutable;

final readonly class Pain001
{
    /**
     * @param array<int, array{
     *   end_to_end_id: string,
     *   amount: string,
     *   currency: string,
     *   creditor_name: string,
     *   creditor_iban: string,
     *   creditor_bic?: string,
     *   remittance_info?: string,
     * }> $transactions
     */
    public function __construct(
        public string $messageId,
        public DateTimeImmutable $creationDateTime,
        public int $numberOfTransactions,
        public string $controlSum,
        public string $initiatingPartyName,
        public string $debtorName,
        public string $debtorIban,
        public string $debtorBic,
        public string $paymentMethod,
        public array $transactions,
    ) {
    }

    /** @param array<string, mixed> $data */
    public static function fromArray(array $data): self
    {
        return new self(
            messageId: $data['message_id'],
            creationDateTime: new DateTimeImmutable($data['creation_date_time']),
            numberOfTransactions: (int) $data['number_of_transactions'],
            controlSum: $data['control_sum'],
            initiatingPartyName: $data['initiating_party_name'],
            debtorName: $data['debtor_name'],
            debtorIban: $data['debtor_iban'],
            debtorBic: $data['debtor_bic'],
            paymentMethod: $data['payment_method'] ?? 'TRF',
            transactions: $data['transactions'],
        );
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        return [
            'message_id' => $this->messageId,
            'creation_date_time' => $this->creationDateTime->format('Y-m-d\TH:i:s.vP'),
            'number_of_transactions' => $this->numberOfTransactions,
            'control_sum' => $this->controlSum,
            'initiating_party_name' => $this->initiatingPartyName,
            'debtor_name' => $this->debtorName,
            'debtor_iban' => $this->debtorIban,
            'debtor_bic' => $this->debtorBic,
            'payment_method' => $this->paymentMethod,
            'transactions' => $this->transactions,
        ];
    }

    public function toXml(): string
    {
        $txXml = '';
        foreach ($this->transactions as $tx) {
            $remittance = isset($tx['remittance_info'])
                ? "<RmtInf><Ustrd>{$tx['remittance_info']}</Ustrd></RmtInf>"
                : '';
            $creditorBic = isset($tx['creditor_bic'])
                ? "<CdtrAgt><FinInstnId><BICFI>{$tx['creditor_bic']}</BICFI></FinInstnId></CdtrAgt>"
                : '';
            $txXml .= "<CdtTrfTxInf><PmtId><EndToEndId>{$tx['end_to_end_id']}</EndToEndId></PmtId>"
                . "<Amt><InstdAmt Ccy=\"{$tx['currency']}\">{$tx['amount']}</InstdAmt></Amt>"
                . $creditorBic
                . "<Cdtr><Nm>{$tx['creditor_name']}</Nm></Cdtr>"
                . "<CdtrAcct><Id><IBAN>{$tx['creditor_iban']}</IBAN></Id></CdtrAcct>"
                . $remittance
                . "</CdtTrfTxInf>";
        }

        return '<?xml version="1.0" encoding="UTF-8"?>'
            . '<Document xmlns="urn:iso:std:iso:20022:tech:xsd:pain.001.001.09">'
            . '<CstmrCdtTrfInitn>'
            . "<GrpHdr><MsgId>{$this->messageId}</MsgId>"
            . "<CreDtTm>{$this->creationDateTime->format('Y-m-d\TH:i:s')}</CreDtTm>"
            . "<NbOfTxs>{$this->numberOfTransactions}</NbOfTxs>"
            . "<CtrlSum>{$this->controlSum}</CtrlSum>"
            . "<InitgPty><Nm>{$this->initiatingPartyName}</Nm></InitgPty></GrpHdr>"
            . "<PmtInf><PmtMtd>{$this->paymentMethod}</PmtMtd>"
            . "<Dbtr><Nm>{$this->debtorName}</Nm></Dbtr>"
            . "<DbtrAcct><Id><IBAN>{$this->debtorIban}</IBAN></Id></DbtrAcct>"
            . "<DbtrAgt><FinInstnId><BICFI>{$this->debtorBic}</BICFI></FinInstnId></DbtrAgt>"
            . $txXml
            . '</PmtInf></CstmrCdtTrfInitn></Document>';
    }
}
```

- [ ] **Step 2: Create Pacs008 (FI-to-FI Credit Transfer)**

```php
<?php
// app/Domain/ISO20022/ValueObjects/Pacs008.php
declare(strict_types=1);

namespace App\Domain\ISO20022\ValueObjects;

use App\Domain\ISO20022\Enums\TransactionStatus;
use DateTimeImmutable;

final readonly class Pacs008
{
    public function __construct(
        public string $messageId,
        public DateTimeImmutable $creationDateTime,
        public int $numberOfTransactions,
        public string $settlementMethod,
        public string $instructingAgentBic,
        public string $instructedAgentBic,
        public string $endToEndId,
        public string $uetr,
        public string $amount,
        public string $currency,
        public string $debtorName,
        public string $debtorIban,
        public string $creditorName,
        public string $creditorIban,
        public ?string $chargeBearer = 'SLEV',
    ) {
    }

    /** @param array<string, mixed> $data */
    public static function fromArray(array $data): self
    {
        return new self(
            messageId: $data['message_id'],
            creationDateTime: new DateTimeImmutable($data['creation_date_time']),
            numberOfTransactions: (int) ($data['number_of_transactions'] ?? 1),
            settlementMethod: $data['settlement_method'] ?? 'CLRG',
            instructingAgentBic: $data['instructing_agent_bic'],
            instructedAgentBic: $data['instructed_agent_bic'],
            endToEndId: $data['end_to_end_id'],
            uetr: $data['uetr'],
            amount: $data['amount'],
            currency: $data['currency'],
            debtorName: $data['debtor_name'],
            debtorIban: $data['debtor_iban'],
            creditorName: $data['creditor_name'],
            creditorIban: $data['creditor_iban'],
            chargeBearer: $data['charge_bearer'] ?? 'SLEV',
        );
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        return [
            'message_id' => $this->messageId,
            'creation_date_time' => $this->creationDateTime->format('Y-m-d\TH:i:s.vP'),
            'number_of_transactions' => $this->numberOfTransactions,
            'settlement_method' => $this->settlementMethod,
            'instructing_agent_bic' => $this->instructingAgentBic,
            'instructed_agent_bic' => $this->instructedAgentBic,
            'end_to_end_id' => $this->endToEndId,
            'uetr' => $this->uetr,
            'amount' => $this->amount,
            'currency' => $this->currency,
            'debtor_name' => $this->debtorName,
            'debtor_iban' => $this->debtorIban,
            'creditor_name' => $this->creditorName,
            'creditor_iban' => $this->creditorIban,
            'charge_bearer' => $this->chargeBearer,
        ];
    }

    public function toXml(): string
    {
        return '<?xml version="1.0" encoding="UTF-8"?>'
            . '<Document xmlns="urn:iso:std:iso:20022:tech:xsd:pacs.008.001.08">'
            . '<FIToFICstmrCdtTrf>'
            . "<GrpHdr><MsgId>{$this->messageId}</MsgId>"
            . "<CreDtTm>{$this->creationDateTime->format('Y-m-d\TH:i:s')}</CreDtTm>"
            . "<NbOfTxs>{$this->numberOfTransactions}</NbOfTxs>"
            . "<SttlmInf><SttlmMtd>{$this->settlementMethod}</SttlmMtd></SttlmInf>"
            . "<InstgAgt><FinInstnId><BICFI>{$this->instructingAgentBic}</BICFI></FinInstnId></InstgAgt>"
            . "<InstdAgt><FinInstnId><BICFI>{$this->instructedAgentBic}</BICFI></FinInstnId></InstdAgt></GrpHdr>"
            . "<CdtTrfTxInf><PmtId><EndToEndId>{$this->endToEndId}</EndToEndId>"
            . "<UETR>{$this->uetr}</UETR></PmtId>"
            . "<IntrBkSttlmAmt Ccy=\"{$this->currency}\">{$this->amount}</IntrBkSttlmAmt>"
            . "<ChrgBr>{$this->chargeBearer}</ChrgBr>"
            . "<Dbtr><Nm>{$this->debtorName}</Nm></Dbtr>"
            . "<DbtrAcct><Id><IBAN>{$this->debtorIban}</IBAN></Id></DbtrAcct>"
            . "<Cdtr><Nm>{$this->creditorName}</Nm></Cdtr>"
            . "<CdtrAcct><Id><IBAN>{$this->creditorIban}</IBAN></Id></CdtrAcct>"
            . '</CdtTrfTxInf></FIToFICstmrCdtTrf></Document>';
    }
}
```

- [ ] **Step 3: Create Pacs002 (Payment Status Report)**

```php
<?php
// app/Domain/ISO20022/ValueObjects/Pacs002.php
declare(strict_types=1);

namespace App\Domain\ISO20022\ValueObjects;

use App\Domain\ISO20022\Enums\TransactionStatus;
use DateTimeImmutable;

final readonly class Pacs002
{
    /**
     * @param array<int, array{
     *   original_end_to_end_id: string,
     *   status: string,
     *   reason_code?: string,
     *   reason_info?: string,
     * }> $transactionStatuses
     */
    public function __construct(
        public string $messageId,
        public DateTimeImmutable $creationDateTime,
        public string $originalMessageId,
        public string $originalMessageType,
        public string $groupStatus,
        public array $transactionStatuses,
    ) {
    }

    /** @param array<string, mixed> $data */
    public static function fromArray(array $data): self
    {
        return new self(
            messageId: $data['message_id'],
            creationDateTime: new DateTimeImmutable($data['creation_date_time']),
            originalMessageId: $data['original_message_id'],
            originalMessageType: $data['original_message_type'],
            groupStatus: $data['group_status'],
            transactionStatuses: $data['transaction_statuses'] ?? [],
        );
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        return [
            'message_id' => $this->messageId,
            'creation_date_time' => $this->creationDateTime->format('Y-m-d\TH:i:s.vP'),
            'original_message_id' => $this->originalMessageId,
            'original_message_type' => $this->originalMessageType,
            'group_status' => $this->groupStatus,
            'transaction_statuses' => $this->transactionStatuses,
        ];
    }

    public function toXml(): string
    {
        $txStatusXml = '';
        foreach ($this->transactionStatuses as $tx) {
            $reason = isset($tx['reason_code'])
                ? "<StsRsnInf><Rsn><Cd>{$tx['reason_code']}</Cd></Rsn><AddtlInf>" . ($tx['reason_info'] ?? '') . '</AddtlInf></StsRsnInf>'
                : '';
            $txStatusXml .= "<TxInfAndSts><OrgnlEndToEndId>{$tx['original_end_to_end_id']}</OrgnlEndToEndId>"
                . "<TxSts>{$tx['status']}</TxSts>{$reason}</TxInfAndSts>";
        }

        return '<?xml version="1.0" encoding="UTF-8"?>'
            . '<Document xmlns="urn:iso:std:iso:20022:tech:xsd:pacs.002.001.10">'
            . '<FIToFIPmtStsRpt>'
            . "<GrpHdr><MsgId>{$this->messageId}</MsgId>"
            . "<CreDtTm>{$this->creationDateTime->format('Y-m-d\TH:i:s')}</CreDtTm></GrpHdr>"
            . "<OrgnlGrpInfAndSts><OrgnlMsgId>{$this->originalMessageId}</OrgnlMsgId>"
            . "<OrgnlMsgNmId>{$this->originalMessageType}</OrgnlMsgNmId>"
            . "<GrpSts>{$this->groupStatus}</GrpSts></OrgnlGrpInfAndSts>"
            . $txStatusXml
            . '</FIToFIPmtStsRpt></Document>';
    }
}
```

- [ ] **Step 4: Create remaining DTOs (Pain008, Pacs003, Pacs004, Camt053, Camt054)**

Each follows the same pattern. Create all 5 files:

**Pain008** — Customer Direct Debit Initiation (mandate_id, creditor_id, debtor_iban, collection_date, amount, currency)

**Pacs003** — FI-to-FI Direct Debit (mandate_id, creditor_agent_bic, debtor_agent_bic, amount, currency)

**Pacs004** — Payment Return (original_message_id, original_end_to_end_id, returned_amount, return_reason_code)

**Camt053** — Bank-to-Customer Statement (account_iban, from_date, to_date, opening_balance, closing_balance, entries array with amount/credit_debit/booking_date/reference)

**Camt054** — Bank-to-Customer Debit/Credit Notification (account_iban, notification_id, entries array — same structure as Camt053 but for real-time notifications)

All must have: `__construct()`, `static fromArray()`, `toArray()`, `toXml()`.

- [ ] **Step 5: Write VO tests**

```php
<?php
// tests/Unit/Domain/ISO20022/ValueObjects/Pain001Test.php
declare(strict_types=1);

use App\Domain\ISO20022\ValueObjects\Pain001;
use App\Domain\ISO20022\ValueObjects\Pacs008;
use App\Domain\ISO20022\ValueObjects\Pacs002;

it('creates Pain001 from array', function (): void {
    $data = [
        'message_id' => 'MSG-001',
        'creation_date_time' => '2026-03-29T10:00:00+00:00',
        'number_of_transactions' => 1,
        'control_sum' => '1000.00',
        'initiating_party_name' => 'Test Corp',
        'debtor_name' => 'Test Corp',
        'debtor_iban' => 'DE89370400440532013000',
        'debtor_bic' => 'COBADEFFXXX',
        'payment_method' => 'TRF',
        'transactions' => [
            [
                'end_to_end_id' => 'E2E-001',
                'amount' => '1000.00',
                'currency' => 'EUR',
                'creditor_name' => 'Creditor GmbH',
                'creditor_iban' => 'GB29NWBK60161331926819',
            ],
        ],
    ];

    $pain001 = Pain001::fromArray($data);
    expect($pain001->messageId)->toBe('MSG-001');
    expect($pain001->numberOfTransactions)->toBe(1);
    expect($pain001->debtorIban)->toBe('DE89370400440532013000');
    expect($pain001->transactions)->toHaveCount(1);
});

it('generates valid Pain001 XML', function (): void {
    $pain001 = Pain001::fromArray([
        'message_id' => 'MSG-001',
        'creation_date_time' => '2026-03-29T10:00:00+00:00',
        'number_of_transactions' => 1,
        'control_sum' => '500.00',
        'initiating_party_name' => 'Sender',
        'debtor_name' => 'Sender',
        'debtor_iban' => 'DE89370400440532013000',
        'debtor_bic' => 'COBADEFFXXX',
        'transactions' => [
            [
                'end_to_end_id' => 'E2E-001',
                'amount' => '500.00',
                'currency' => 'EUR',
                'creditor_name' => 'Receiver',
                'creditor_iban' => 'GB29NWBK60161331926819',
            ],
        ],
    ]);

    $xml = $pain001->toXml();
    expect($xml)->toContain('pain.001.001.09');
    expect($xml)->toContain('<MsgId>MSG-001</MsgId>');
    expect($xml)->toContain('<IBAN>DE89370400440532013000</IBAN>');
    expect($xml)->toContain('Ccy="EUR"');
});

it('round-trips Pain001 through toArray/fromArray', function (): void {
    $original = Pain001::fromArray([
        'message_id' => 'ROUNDTRIP-001',
        'creation_date_time' => '2026-03-29T12:00:00+00:00',
        'number_of_transactions' => 2,
        'control_sum' => '1500.00',
        'initiating_party_name' => 'Initiator',
        'debtor_name' => 'Debtor',
        'debtor_iban' => 'DE89370400440532013000',
        'debtor_bic' => 'COBADEFFXXX',
        'payment_method' => 'TRF',
        'transactions' => [
            ['end_to_end_id' => 'E1', 'amount' => '500.00', 'currency' => 'EUR', 'creditor_name' => 'C1', 'creditor_iban' => 'GB29NWBK60161331926819'],
            ['end_to_end_id' => 'E2', 'amount' => '1000.00', 'currency' => 'EUR', 'creditor_name' => 'C2', 'creditor_iban' => 'FR7630006000011234567890189'],
        ],
    ]);

    $restored = Pain001::fromArray($original->toArray());
    expect($restored->messageId)->toBe($original->messageId);
    expect($restored->numberOfTransactions)->toBe(2);
    expect($restored->transactions)->toHaveCount(2);
});

it('creates Pacs008 from array', function (): void {
    $pacs = Pacs008::fromArray([
        'message_id' => 'PACS-001',
        'creation_date_time' => '2026-03-29T10:00:00+00:00',
        'instructing_agent_bic' => 'BANKUS33',
        'instructed_agent_bic' => 'BANKGB2L',
        'end_to_end_id' => 'E2E-001',
        'uetr' => '97ed4827-7b6f-4491-a06f-b548d5a7512d',
        'amount' => '25000.00',
        'currency' => 'USD',
        'debtor_name' => 'US Corp',
        'debtor_iban' => 'US33BANK12345678901234',
        'creditor_name' => 'UK Ltd',
        'creditor_iban' => 'GB29NWBK60161331926819',
    ]);

    expect($pacs->uetr)->toBe('97ed4827-7b6f-4491-a06f-b548d5a7512d');
    expect($pacs->amount)->toBe('25000.00');
    expect($pacs->toXml())->toContain('pacs.008.001.08');
});

it('creates Pacs002 status report', function (): void {
    $status = Pacs002::fromArray([
        'message_id' => 'STATUS-001',
        'creation_date_time' => '2026-03-29T10:00:00+00:00',
        'original_message_id' => 'PACS-001',
        'original_message_type' => 'pacs.008.001.08',
        'group_status' => 'ACSC',
        'transaction_statuses' => [
            ['original_end_to_end_id' => 'E2E-001', 'status' => 'ACSC'],
        ],
    ]);

    expect($status->groupStatus)->toBe('ACSC');
    expect($status->toXml())->toContain('pacs.002.001.10');
    expect($status->toXml())->toContain('<GrpSts>ACSC</GrpSts>');
});
```

- [ ] **Step 6: Run tests and PHPStan**

Run: `XDEBUG_MODE=off vendor/bin/pest tests/Unit/Domain/ISO20022/`
Expected: All tests pass

Run: `XDEBUG_MODE=off vendor/bin/phpstan analyse --memory-limit=2G app/Domain/ISO20022/`
Expected: No errors

- [ ] **Step 7: Commit**

```bash
git add app/Domain/ISO20022/ValueObjects/ tests/Unit/Domain/ISO20022/ValueObjects/
git commit -m "feat(iso20022): message DTOs — Pain001, Pacs008, Pacs002, Pain008, Pacs003, Pacs004, Camt053, Camt054"
```

---

## Task 3: ISO 20022 — MessageParser, MessageGenerator, MessageValidator, MessageRegistry

**Files:**
- Create: `app/Domain/ISO20022/Services/MessageRegistry.php`
- Create: `app/Domain/ISO20022/Services/MessageParser.php`
- Create: `app/Domain/ISO20022/Services/MessageGenerator.php`
- Create: `app/Domain/ISO20022/Services/MessageValidator.php`
- Test: `tests/Unit/Domain/ISO20022/Services/MessageParserTest.php`
- Test: `tests/Unit/Domain/ISO20022/Services/MessageGeneratorTest.php`
- Test: `tests/Unit/Domain/ISO20022/Services/MessageValidatorTest.php`

- [ ] **Step 1: Create MessageRegistry**

```php
<?php
// app/Domain/ISO20022/Services/MessageRegistry.php
declare(strict_types=1);

namespace App\Domain\ISO20022\Services;

use App\Domain\ISO20022\Enums\MessageFamily;
use App\Domain\ISO20022\ValueObjects\Camt053;
use App\Domain\ISO20022\ValueObjects\Camt054;
use App\Domain\ISO20022\ValueObjects\Pacs002;
use App\Domain\ISO20022\ValueObjects\Pacs003;
use App\Domain\ISO20022\ValueObjects\Pacs004;
use App\Domain\ISO20022\ValueObjects\Pacs008;
use App\Domain\ISO20022\ValueObjects\Pain001;
use App\Domain\ISO20022\ValueObjects\Pain008;

final class MessageRegistry
{
    /** @var array<string, class-string> */
    private const MESSAGE_MAP = [
        'pain.001' => Pain001::class,
        'pain.008' => Pain008::class,
        'pacs.008' => Pacs008::class,
        'pacs.002' => Pacs002::class,
        'pacs.003' => Pacs003::class,
        'pacs.004' => Pacs004::class,
        'camt.053' => Camt053::class,
        'camt.054' => Camt054::class,
    ];

    /** @var array<string, string> */
    private const NAMESPACE_MAP = [
        'pain.001' => 'urn:iso:std:iso:20022:tech:xsd:pain.001.001.09',
        'pain.008' => 'urn:iso:std:iso:20022:tech:xsd:pain.008.001.08',
        'pacs.008' => 'urn:iso:std:iso:20022:tech:xsd:pacs.008.001.08',
        'pacs.002' => 'urn:iso:std:iso:20022:tech:xsd:pacs.002.001.10',
        'pacs.003' => 'urn:iso:std:iso:20022:tech:xsd:pacs.003.001.08',
        'pacs.004' => 'urn:iso:std:iso:20022:tech:xsd:pacs.004.001.09',
        'camt.053' => 'urn:iso:std:iso:20022:tech:xsd:camt.053.001.08',
        'camt.054' => 'urn:iso:std:iso:20022:tech:xsd:camt.054.001.08',
    ];

    /** @return class-string|null */
    public function getDtoClass(string $messageType): ?string
    {
        return self::MESSAGE_MAP[$messageType] ?? null;
    }

    public function getNamespace(string $messageType): ?string
    {
        return self::NAMESPACE_MAP[$messageType] ?? null;
    }

    public function isSupported(string $messageType): bool
    {
        return isset(self::MESSAGE_MAP[$messageType]);
    }

    /** @return array<string> */
    public function supportedTypes(): array
    {
        return array_keys(self::MESSAGE_MAP);
    }

    public function detectMessageType(string $xml): ?string
    {
        foreach (self::NAMESPACE_MAP as $type => $namespace) {
            if (str_contains($xml, $namespace)) {
                return $type;
            }
        }

        return null;
    }
}
```

- [ ] **Step 2: Create MessageParser**

```php
<?php
// app/Domain/ISO20022/Services/MessageParser.php
declare(strict_types=1);

namespace App\Domain\ISO20022\Services;

use RuntimeException;
use SimpleXMLElement;

final class MessageParser
{
    public function __construct(
        private readonly MessageRegistry $registry,
    ) {
    }

    /**
     * Parse an ISO 20022 XML string into the appropriate DTO.
     *
     * @return object The typed DTO (Pain001, Pacs008, etc.)
     */
    public function parseXml(string $xml): object
    {
        $messageType = $this->registry->detectMessageType($xml);
        if ($messageType === null) {
            throw new RuntimeException('Unrecognized ISO 20022 message namespace');
        }

        $dtoClass = $this->registry->getDtoClass($messageType);
        if ($dtoClass === null) {
            throw new RuntimeException("No DTO registered for message type: {$messageType}");
        }

        $data = $this->xmlToArray($xml, $messageType);

        return $dtoClass::fromArray($data);
    }

    /**
     * Parse an ISO 20022 JSON representation into the appropriate DTO.
     *
     * @param array<string, mixed> $data
     * @return object The typed DTO
     */
    public function parseArray(string $messageType, array $data): object
    {
        $dtoClass = $this->registry->getDtoClass($messageType);
        if ($dtoClass === null) {
            throw new RuntimeException("Unsupported message type: {$messageType}");
        }

        return $dtoClass::fromArray($data);
    }

    /**
     * @return array<string, mixed>
     */
    private function xmlToArray(string $xml, string $messageType): array
    {
        $namespace = $this->registry->getNamespace($messageType);
        $sxe = new SimpleXMLElement($xml);
        $sxe->registerXPathNamespace('ns', (string) $namespace);

        return match ($messageType) {
            'pain.001' => $this->parsePain001($sxe, (string) $namespace),
            'pacs.008' => $this->parsePacs008($sxe, (string) $namespace),
            'pacs.002' => $this->parsePacs002($sxe, (string) $namespace),
            default => $this->parseGeneric($sxe),
        };
    }

    /** @return array<string, mixed> */
    private function parsePain001(SimpleXMLElement $sxe, string $ns): array
    {
        $doc = $sxe->children($ns);
        $initn = $doc->CstmrCdtTrfInitn;
        $hdr = $initn->GrpHdr;
        $pmtInf = $initn->PmtInf;

        $transactions = [];
        foreach ($pmtInf->CdtTrfTxInf as $tx) {
            $transactions[] = [
                'end_to_end_id' => (string) $tx->PmtId->EndToEndId,
                'amount' => (string) $tx->Amt->InstdAmt,
                'currency' => (string) $tx->Amt->InstdAmt['Ccy'],
                'creditor_name' => (string) $tx->Cdtr->Nm,
                'creditor_iban' => (string) $tx->CdtrAcct->Id->IBAN,
            ];
        }

        return [
            'message_id' => (string) $hdr->MsgId,
            'creation_date_time' => (string) $hdr->CreDtTm,
            'number_of_transactions' => (int) (string) $hdr->NbOfTxs,
            'control_sum' => (string) $hdr->CtrlSum,
            'initiating_party_name' => (string) $hdr->InitgPty->Nm,
            'debtor_name' => (string) $pmtInf->Dbtr->Nm,
            'debtor_iban' => (string) $pmtInf->DbtrAcct->Id->IBAN,
            'debtor_bic' => (string) $pmtInf->DbtrAgt->FinInstnId->BICFI,
            'payment_method' => (string) $pmtInf->PmtMtd,
            'transactions' => $transactions,
        ];
    }

    /** @return array<string, mixed> */
    private function parsePacs008(SimpleXMLElement $sxe, string $ns): array
    {
        $doc = $sxe->children($ns);
        $transfer = $doc->FIToFICstmrCdtTrf;
        $hdr = $transfer->GrpHdr;
        $tx = $transfer->CdtTrfTxInf;

        return [
            'message_id' => (string) $hdr->MsgId,
            'creation_date_time' => (string) $hdr->CreDtTm,
            'number_of_transactions' => (int) (string) $hdr->NbOfTxs,
            'settlement_method' => (string) $hdr->SttlmInf->SttlmMtd,
            'instructing_agent_bic' => (string) $hdr->InstgAgt->FinInstnId->BICFI,
            'instructed_agent_bic' => (string) $hdr->InstdAgt->FinInstnId->BICFI,
            'end_to_end_id' => (string) $tx->PmtId->EndToEndId,
            'uetr' => (string) $tx->PmtId->UETR,
            'amount' => (string) $tx->IntrBkSttlmAmt,
            'currency' => (string) $tx->IntrBkSttlmAmt['Ccy'],
            'debtor_name' => (string) $tx->Dbtr->Nm,
            'debtor_iban' => (string) $tx->DbtrAcct->Id->IBAN,
            'creditor_name' => (string) $tx->Cdtr->Nm,
            'creditor_iban' => (string) $tx->CdtrAcct->Id->IBAN,
            'charge_bearer' => (string) $tx->ChrgBr,
        ];
    }

    /** @return array<string, mixed> */
    private function parsePacs002(SimpleXMLElement $sxe, string $ns): array
    {
        $doc = $sxe->children($ns);
        $report = $doc->FIToFIPmtStsRpt;
        $hdr = $report->GrpHdr;
        $orig = $report->OrgnlGrpInfAndSts;

        $statuses = [];
        foreach ($report->TxInfAndSts as $tx) {
            $statuses[] = [
                'original_end_to_end_id' => (string) $tx->OrgnlEndToEndId,
                'status' => (string) $tx->TxSts,
            ];
        }

        return [
            'message_id' => (string) $hdr->MsgId,
            'creation_date_time' => (string) $hdr->CreDtTm,
            'original_message_id' => (string) $orig->OrgnlMsgId,
            'original_message_type' => (string) $orig->OrgnlMsgNmId,
            'group_status' => (string) $orig->GrpSts,
            'transaction_statuses' => $statuses,
        ];
    }

    /** @return array<string, mixed> */
    private function parseGeneric(SimpleXMLElement $sxe): array
    {
        return json_decode((string) json_encode($sxe), true) ?: [];
    }
}
```

- [ ] **Step 3: Create MessageGenerator**

```php
<?php
// app/Domain/ISO20022/Services/MessageGenerator.php
declare(strict_types=1);

namespace App\Domain\ISO20022\Services;

use RuntimeException;

final class MessageGenerator
{
    public function __construct(
        private readonly MessageRegistry $registry,
    ) {
    }

    /**
     * Generate ISO 20022 XML from a DTO.
     */
    public function toXml(object $dto): string
    {
        if (! method_exists($dto, 'toXml')) {
            throw new RuntimeException('DTO does not implement toXml(): ' . $dto::class);
        }

        return $dto->toXml();
    }

    /**
     * Generate ISO 20022 message from array data.
     */
    public function generateFromArray(string $messageType, array $data): string
    {
        $dtoClass = $this->registry->getDtoClass($messageType);
        if ($dtoClass === null) {
            throw new RuntimeException("Unsupported message type: {$messageType}");
        }

        $dto = $dtoClass::fromArray($data);

        return $dto->toXml();
    }

    /**
     * Generate an array representation from a DTO.
     *
     * @return array<string, mixed>
     */
    public function toArray(object $dto): array
    {
        if (! method_exists($dto, 'toArray')) {
            throw new RuntimeException('DTO does not implement toArray(): ' . $dto::class);
        }

        return $dto->toArray();
    }
}
```

- [ ] **Step 4: Create MessageValidator**

```php
<?php
// app/Domain/ISO20022/Services/MessageValidator.php
declare(strict_types=1);

namespace App\Domain\ISO20022\Services;

use RuntimeException;

final class MessageValidator
{
    public function __construct(
        private readonly MessageRegistry $registry,
    ) {
    }

    /**
     * Validate an ISO 20022 XML message.
     *
     * @return array{valid: bool, errors: array<string>}
     */
    public function validate(string $xml): array
    {
        $errors = [];

        // Check well-formed XML
        libxml_use_internal_errors(true);
        $sxe = simplexml_load_string($xml);
        if ($sxe === false) {
            foreach (libxml_get_errors() as $error) {
                $errors[] = "XML parse error line {$error->line}: " . trim($error->message);
            }
            libxml_clear_errors();

            return ['valid' => false, 'errors' => $errors];
        }
        libxml_clear_errors();

        // Detect message type
        $messageType = $this->registry->detectMessageType($xml);
        if ($messageType === null) {
            return ['valid' => false, 'errors' => ['Unrecognized ISO 20022 message namespace']];
        }

        // Check enabled families
        $family = explode('.', $messageType)[0] ?? '';
        $enabledFamilies = (array) config('iso20022.enabled_families', []);
        if (! in_array($family, $enabledFamilies, true)) {
            return ['valid' => false, 'errors' => ["Message family '{$family}' is not enabled"]];
        }

        // Check message size
        $maxSizeBytes = ((int) config('iso20022.max_message_size_kb', 512)) * 1024;
        if (strlen($xml) > $maxSizeBytes) {
            $errors[] = 'Message exceeds maximum size of ' . config('iso20022.max_message_size_kb') . 'KB';
        }

        // Validate required fields based on message type
        $fieldErrors = $this->validateRequiredFields($sxe, $messageType);
        $errors = array_merge($errors, $fieldErrors);

        return ['valid' => $errors === [], 'errors' => $errors];
    }

    /** @return array<string> */
    private function validateRequiredFields(\SimpleXMLElement $sxe, string $messageType): array
    {
        $ns = $this->registry->getNamespace($messageType);
        $doc = $sxe->children((string) $ns);
        $errors = [];

        $requiredPaths = match ($messageType) {
            'pain.001' => ['CstmrCdtTrfInitn.GrpHdr.MsgId', 'CstmrCdtTrfInitn.GrpHdr.NbOfTxs'],
            'pacs.008' => ['FIToFICstmrCdtTrf.GrpHdr.MsgId', 'FIToFICstmrCdtTrf.GrpHdr.NbOfTxs'],
            'pacs.002' => ['FIToFIPmtStsRpt.GrpHdr.MsgId'],
            default => [],
        };

        foreach ($requiredPaths as $path) {
            $parts = explode('.', $path);
            $node = $doc;
            foreach ($parts as $part) {
                $node = $node->{$part};
            }
            if ((string) $node === '') {
                $errors[] = "Required field missing: {$path}";
            }
        }

        return $errors;
    }
}
```

- [ ] **Step 5: Write service tests**

```php
<?php
// tests/Unit/Domain/ISO20022/Services/MessageParserTest.php
declare(strict_types=1);

use App\Domain\ISO20022\Services\MessageParser;
use App\Domain\ISO20022\Services\MessageRegistry;
use App\Domain\ISO20022\ValueObjects\Pain001;
use App\Domain\ISO20022\ValueObjects\Pacs008;

beforeEach(function (): void {
    $this->registry = new MessageRegistry();
    $this->parser = new MessageParser($this->registry);
});

it('parses Pain001 XML into DTO', function (): void {
    $xml = Pain001::fromArray([
        'message_id' => 'PARSE-001',
        'creation_date_time' => '2026-03-29T10:00:00',
        'number_of_transactions' => 1,
        'control_sum' => '500.00',
        'initiating_party_name' => 'Sender',
        'debtor_name' => 'Sender',
        'debtor_iban' => 'DE89370400440532013000',
        'debtor_bic' => 'COBADEFFXXX',
        'transactions' => [
            ['end_to_end_id' => 'E2E-001', 'amount' => '500.00', 'currency' => 'EUR', 'creditor_name' => 'Receiver', 'creditor_iban' => 'GB29NWBK60161331926819'],
        ],
    ])->toXml();

    $dto = $this->parser->parseXml($xml);
    expect($dto)->toBeInstanceOf(Pain001::class);
    expect($dto->messageId)->toBe('PARSE-001');
    expect($dto->debtorIban)->toBe('DE89370400440532013000');
});

it('parses Pacs008 XML into DTO', function (): void {
    $xml = Pacs008::fromArray([
        'message_id' => 'PACS-PARSE-001',
        'creation_date_time' => '2026-03-29T10:00:00',
        'instructing_agent_bic' => 'BANKUS33',
        'instructed_agent_bic' => 'BANKGB2L',
        'end_to_end_id' => 'E2E-002',
        'uetr' => '97ed4827-7b6f-4491-a06f-b548d5a7512d',
        'amount' => '10000.00',
        'currency' => 'USD',
        'debtor_name' => 'US Debtor',
        'debtor_iban' => 'US33BANK12345678901234',
        'creditor_name' => 'UK Creditor',
        'creditor_iban' => 'GB29NWBK60161331926819',
    ])->toXml();

    $dto = $this->parser->parseXml($xml);
    expect($dto)->toBeInstanceOf(Pacs008::class);
    expect($dto->uetr)->toBe('97ed4827-7b6f-4491-a06f-b548d5a7512d');
});

it('throws on unrecognized namespace', function (): void {
    $this->parser->parseXml('<?xml version="1.0"?><Document xmlns="urn:unknown"><Test/></Document>');
})->throws(RuntimeException::class, 'Unrecognized ISO 20022 message namespace');

it('parses array into DTO by message type', function (): void {
    $dto = $this->parser->parseArray('pain.001', [
        'message_id' => 'ARR-001',
        'creation_date_time' => '2026-03-29T10:00:00+00:00',
        'number_of_transactions' => 0,
        'control_sum' => '0.00',
        'initiating_party_name' => 'Test',
        'debtor_name' => 'Test',
        'debtor_iban' => 'DE89370400440532013000',
        'debtor_bic' => 'COBADEFFXXX',
        'transactions' => [],
    ]);
    expect($dto)->toBeInstanceOf(Pain001::class);
});
```

```php
<?php
// tests/Unit/Domain/ISO20022/Services/MessageValidatorTest.php
declare(strict_types=1);

use App\Domain\ISO20022\Services\MessageRegistry;
use App\Domain\ISO20022\Services\MessageValidator;
use App\Domain\ISO20022\ValueObjects\Pain001;

uses(Tests\TestCase::class);

beforeEach(function (): void {
    $this->validator = new MessageValidator(new MessageRegistry());
});

it('validates well-formed Pain001 XML', function (): void {
    $xml = Pain001::fromArray([
        'message_id' => 'VALID-001',
        'creation_date_time' => '2026-03-29T10:00:00',
        'number_of_transactions' => 1,
        'control_sum' => '100.00',
        'initiating_party_name' => 'Sender',
        'debtor_name' => 'Sender',
        'debtor_iban' => 'DE89370400440532013000',
        'debtor_bic' => 'COBADEFFXXX',
        'transactions' => [
            ['end_to_end_id' => 'E1', 'amount' => '100.00', 'currency' => 'EUR', 'creditor_name' => 'R', 'creditor_iban' => 'GB29NWBK60161331926819'],
        ],
    ])->toXml();

    $result = $this->validator->validate($xml);
    expect($result['valid'])->toBeTrue();
    expect($result['errors'])->toBeEmpty();
});

it('rejects malformed XML', function (): void {
    $result = $this->validator->validate('<not-xml<broken>');
    expect($result['valid'])->toBeFalse();
    expect($result['errors'])->not->toBeEmpty();
});

it('rejects unrecognized namespace', function (): void {
    $result = $this->validator->validate('<?xml version="1.0"?><Document xmlns="urn:fake"><X/></Document>');
    expect($result['valid'])->toBeFalse();
    expect($result['errors'][0])->toContain('Unrecognized');
});

it('rejects disabled message family', function (): void {
    config(['iso20022.enabled_families' => ['camt']]);
    $xml = Pain001::fromArray([
        'message_id' => 'DISABLED-001',
        'creation_date_time' => '2026-03-29T10:00:00',
        'number_of_transactions' => 1,
        'control_sum' => '0',
        'initiating_party_name' => 'X',
        'debtor_name' => 'X',
        'debtor_iban' => 'DE89370400440532013000',
        'debtor_bic' => 'COBADEFFXXX',
        'transactions' => [],
    ])->toXml();

    $result = $this->validator->validate($xml);
    expect($result['valid'])->toBeFalse();
    expect($result['errors'][0])->toContain('not enabled');
});
```

- [ ] **Step 6: Run tests and PHPStan**

Run: `XDEBUG_MODE=off vendor/bin/pest tests/Unit/Domain/ISO20022/`
Expected: All tests pass

Run: `XDEBUG_MODE=off vendor/bin/phpstan analyse --memory-limit=2G app/Domain/ISO20022/`
Expected: No errors

- [ ] **Step 7: Commit**

```bash
git add app/Domain/ISO20022/Services/ tests/Unit/Domain/ISO20022/Services/
git commit -m "feat(iso20022): message parser, generator, validator, and registry services"
```

---

## Task 4: ISO 20022 — Routes and GraphQL

**Files:**
- Create: `app/Domain/ISO20022/Routes/api.php`
- Create: `graphql/iso20022.graphql`
- Create: `app/GraphQL/Queries/ISO20022/ValidateMessageQuery.php`
- Create: `app/GraphQL/Queries/ISO20022/MessageStatusQuery.php`
- Modify: `graphql/schema.graphql` — add `#import iso20022.graphql`

- [ ] **Step 1: Create REST routes**

```php
<?php
// app/Domain/ISO20022/Routes/api.php
declare(strict_types=1);

use Illuminate\Http\JsonResponse;
use Illuminate\Http\Request;
use Illuminate\Support\Facades\Route;
use App\Domain\ISO20022\Services\MessageParser;
use App\Domain\ISO20022\Services\MessageGenerator;
use App\Domain\ISO20022\Services\MessageValidator;
use App\Domain\ISO20022\Services\MessageRegistry;

Route::prefix('v1/iso20022')->name('api.iso20022.')->middleware(['auth:sanctum'])->group(function (): void {
    Route::post('/validate', function (Request $request): JsonResponse {
        $request->validate(['xml' => ['required', 'string']]);
        $validator = app(MessageValidator::class);
        $result = $validator->validate($request->input('xml'));

        return response()->json(['success' => true, 'data' => $result]);
    })->name('validate');

    Route::post('/parse', function (Request $request): JsonResponse {
        $request->validate(['xml' => ['required', 'string']]);
        $parser = app(MessageParser::class);
        $dto = $parser->parseXml($request->input('xml'));
        $generator = app(MessageGenerator::class);

        return response()->json(['success' => true, 'data' => $generator->toArray($dto)]);
    })->name('parse');

    Route::post('/generate', function (Request $request): JsonResponse {
        $request->validate([
            'message_type' => ['required', 'string'],
            'data' => ['required', 'array'],
        ]);
        $generator = app(MessageGenerator::class);
        $xml = $generator->generateFromArray(
            $request->input('message_type'),
            $request->input('data'),
        );

        return response()->json(['success' => true, 'data' => ['xml' => $xml]]);
    })->name('generate');

    Route::get('/supported-types', function (): JsonResponse {
        $registry = app(MessageRegistry::class);

        return response()->json(['success' => true, 'data' => $registry->supportedTypes()]);
    })->name('supported-types');
});
```

- [ ] **Step 2: Create GraphQL schema**

```graphql
# graphql/iso20022.graphql

type Iso20022ValidationResult {
    valid: Boolean!
    errors: [String!]!
}

type Iso20022ParsedMessage {
    message_type: String!
    data: JSON!
}

extend type Query {
    iso20022SupportedTypes: [String!]!
        @guard(with: ["sanctum"])
        @field(resolver: "App\\GraphQL\\Queries\\ISO20022\\SupportedTypesQuery")
}

extend type Mutation {
    iso20022Validate(xml: String!): Iso20022ValidationResult!
        @guard(with: ["sanctum"])
        @field(resolver: "App\\GraphQL\\Queries\\ISO20022\\ValidateMessageQuery")
}
```

- [ ] **Step 3: Create GraphQL resolvers**

```php
<?php
// app/GraphQL/Queries/ISO20022/ValidateMessageQuery.php
declare(strict_types=1);

namespace App\GraphQL\Queries\ISO20022;

use App\Domain\ISO20022\Services\MessageValidator;

final class ValidateMessageQuery
{
    public function __construct(
        private readonly MessageValidator $validator,
    ) {
    }

    /** @return array{valid: bool, errors: array<string>} */
    public function __invoke(mixed $rootValue, array $args): array
    {
        return $this->validator->validate($args['xml']);
    }
}
```

```php
<?php
// app/GraphQL/Queries/ISO20022/SupportedTypesQuery.php
declare(strict_types=1);

namespace App\GraphQL\Queries\ISO20022;

use App\Domain\ISO20022\Services\MessageRegistry;

final class SupportedTypesQuery
{
    public function __construct(
        private readonly MessageRegistry $registry,
    ) {
    }

    /** @return array<string> */
    public function __invoke(mixed $rootValue, array $args): array
    {
        return $this->registry->supportedTypes();
    }
}
```

- [ ] **Step 4: Add import to schema.graphql**

Add `#import iso20022.graphql` to `graphql/schema.graphql` alongside the other imports.

- [ ] **Step 5: Run PHPStan, php-cs-fixer**

Run: `./vendor/bin/php-cs-fixer fix --config=.php-cs-fixer.php`
Run: `XDEBUG_MODE=off vendor/bin/phpstan analyse --memory-limit=2G app/Domain/ISO20022/ app/GraphQL/Queries/ISO20022/`
Expected: No errors

- [ ] **Step 6: Commit**

```bash
git add app/Domain/ISO20022/Routes/ graphql/iso20022.graphql graphql/schema.graphql app/GraphQL/Queries/ISO20022/
git commit -m "feat(iso20022): REST routes, GraphQL schema, and resolvers"
```

---

## Task 5: Open Banking — Config, Enums, Models, Migrations

**Files:**
- Create: `config/openbanking.php`
- Create: `app/Domain/OpenBanking/Enums/ConsentStatus.php`
- Create: `app/Domain/OpenBanking/Enums/ConsentPermission.php`
- Create: `app/Domain/OpenBanking/Enums/OpenBankingStandard.php`
- Create: `app/Domain/OpenBanking/Models/Consent.php`
- Create: `app/Domain/OpenBanking/Models/TppRegistration.php`
- Create: `app/Domain/OpenBanking/Models/ConsentAccessLog.php`
- Create: `database/migrations/2026_03_29_000001_create_consents_table.php`
- Create: `database/migrations/2026_03_29_000002_create_tpp_registrations_table.php`
- Create: `database/migrations/2026_03_29_000003_create_consent_access_logs_table.php`
- Test: `tests/Unit/Domain/OpenBanking/Enums/ConsentStatusTest.php`

This task creates the data layer. Services come in Task 6.

- [ ] **Step 1: Create config**

```php
<?php
// config/openbanking.php
declare(strict_types=1);

return [
    'enabled' => env('OPEN_BANKING_ENABLED', false),
    'standard' => env('OPEN_BANKING_STANDARD', 'berlin_group'),
    'consent_max_days' => (int) env('OPEN_BANKING_CONSENT_MAX_DAYS', 90),
    'frequency_per_day' => (int) env('OPEN_BANKING_FREQUENCY', 4),
    'require_sca' => (bool) env('OPEN_BANKING_REQUIRE_SCA', true),
    'tpp_certificate_validation' => (bool) env('OPEN_BANKING_VALIDATE_CERTS', true),
    'supported_permissions' => [
        'ReadAccountsBasic',
        'ReadAccountsDetail',
        'ReadBalances',
        'ReadTransactionsBasic',
        'ReadTransactionsDetail',
        'ReadTransactionsCredits',
        'ReadTransactionsDebits',
    ],
];
```

- [ ] **Step 2: Create enums**

ConsentStatus: `awaiting_authorization`, `authorized`, `rejected`, `revoked`, `expired`
ConsentPermission: cases matching config supported_permissions
OpenBankingStandard: `berlin_group`, `uk_ob`

Each enum with `label()` method returning human-readable name.

- [ ] **Step 3: Create migrations**

Consents table: `id` (uuid primary), `tpp_id` (foreign to tpp_registrations), `user_id` (foreignId), `status` (string), `permissions` (json), `account_ids` (json nullable), `expires_at` (timestamp), `frequency_per_day` (int default 4), `recurring_indicator` (bool default false), `authorized_at` (timestamp nullable), `revoked_at` (timestamp nullable), `timestamps`.

TppRegistrations table: `id` (uuid primary), `tpp_id` (string unique), `name` (string), `client_id` (string unique), `client_secret_hash` (string), `eidas_certificate` (text nullable), `redirect_uris` (json), `roles` (json — array of AISP/PISP/CBPII), `status` (string default 'active'), `timestamps`.

ConsentAccessLogs table: `id` (bigIncrements), `consent_id` (uuid foreign), `tpp_id` (string), `endpoint` (string), `ip_address` (string nullable), `timestamps`.

- [ ] **Step 4: Create models**

Each model: `declare(strict_types=1)`, explicit `$table`, `$fillable`, `$casts` with enums where applicable, uuid primary key via `HasUuids` trait for Consent and TppRegistration.

Consent model includes scopes: `scopeActive()`, `scopeForUser()`, `scopeForTpp()`. Relationship: `belongsTo(TppRegistration::class)`.

- [ ] **Step 5: Write tests**

```php
<?php
// tests/Unit/Domain/OpenBanking/Enums/ConsentStatusTest.php
declare(strict_types=1);

use App\Domain\OpenBanking\Enums\ConsentStatus;

it('has all consent lifecycle statuses', function (): void {
    expect(ConsentStatus::cases())->toHaveCount(5);
    expect(ConsentStatus::AWAITING_AUTHORIZATION->value)->toBe('awaiting_authorization');
    expect(ConsentStatus::AUTHORIZED->value)->toBe('authorized');
    expect(ConsentStatus::REVOKED->value)->toBe('revoked');
});

it('identifies terminal statuses', function (): void {
    expect(ConsentStatus::REVOKED->isTerminal())->toBeTrue();
    expect(ConsentStatus::EXPIRED->isTerminal())->toBeTrue();
    expect(ConsentStatus::REJECTED->isTerminal())->toBeTrue();
    expect(ConsentStatus::AUTHORIZED->isTerminal())->toBeFalse();
});
```

- [ ] **Step 6: Run tests, PHPStan**

Run: `XDEBUG_MODE=off vendor/bin/pest tests/Unit/Domain/OpenBanking/`
Run: `XDEBUG_MODE=off vendor/bin/phpstan analyse --memory-limit=2G app/Domain/OpenBanking/ config/openbanking.php`
Expected: All pass

- [ ] **Step 7: Commit**

```bash
git add config/openbanking.php app/Domain/OpenBanking/ database/migrations/2026_03_29_00000* tests/Unit/Domain/OpenBanking/
git commit -m "feat(openbanking): domain scaffolding — config, enums, models, migrations"
```

---

## Task 6: Open Banking — ConsentService and ConsentEnforcementService

**Files:**
- Create: `app/Domain/OpenBanking/Services/ConsentService.php`
- Create: `app/Domain/OpenBanking/Services/ConsentEnforcementService.php`
- Test: `tests/Unit/Domain/OpenBanking/Services/ConsentServiceTest.php`
- Test: `tests/Unit/Domain/OpenBanking/Services/ConsentEnforcementServiceTest.php`

ConsentService manages the consent lifecycle (create, authorize, revoke, check expiry).
ConsentEnforcementService is called by middleware to verify valid consent exists before data access.

- [ ] **Step 1: Create ConsentService**

Methods: `createConsent(string $tppId, int $userId, array $permissions, ?array $accountIds): Consent`, `authorizeConsent(string $consentId, int $userId): Consent`, `rejectConsent(string $consentId, int $userId): Consent`, `revokeConsent(string $consentId, int $userId): Consent`, `getConsent(string $consentId): ?Consent`, `getActiveConsentsForUser(int $userId): Collection`, `expireStaleConsents(): int`.

Consent creation sets status to `awaiting_authorization`, sets `expires_at` to now + `consent_max_days` config.

- [ ] **Step 2: Create ConsentEnforcementService**

Methods: `validateAccess(string $tppId, int $userId, string $permission, ?string $accountId = null): bool`, `logAccess(string $consentId, string $tppId, string $endpoint, ?string $ipAddress): void`, `checkFrequencyLimit(string $consentId): bool`.

`validateAccess` checks: consent exists, status is authorized, not expired, has required permission, account_id in scope if specified, frequency limit not exceeded.

- [ ] **Step 3: Write tests**

Unit tests using Mockery for model interactions. Tests cover: consent creation, authorization flow, rejection, revocation, expiry check, enforcement with valid/invalid/expired/wrong-permission consent, frequency limiting.

- [ ] **Step 4: Run tests, PHPStan, commit**

```bash
git add app/Domain/OpenBanking/Services/ConsentService.php app/Domain/OpenBanking/Services/ConsentEnforcementService.php tests/Unit/Domain/OpenBanking/Services/
git commit -m "feat(openbanking): consent service and enforcement service"
```

---

## Task 7: Open Banking — AISP, PISP, TPP Registration, and Adapters

**Files:**
- Create: `app/Domain/OpenBanking/Services/AispService.php`
- Create: `app/Domain/OpenBanking/Services/PispService.php`
- Create: `app/Domain/OpenBanking/Services/TppRegistrationService.php`
- Create: `app/Domain/OpenBanking/Services/BerlinGroupAdapter.php`
- Create: `app/Domain/OpenBanking/Services/UkObAdapter.php`
- Test: `tests/Unit/Domain/OpenBanking/Services/AispServiceTest.php`
- Test: `tests/Unit/Domain/OpenBanking/Services/PispServiceTest.php`
- Test: `tests/Unit/Domain/OpenBanking/Services/BerlinGroupAdapterTest.php`

AispService: `getAccounts(string $consentId)`, `getAccountDetail(string $consentId, string $accountId)`, `getBalances(string $consentId, string $accountId)`, `getTransactions(string $consentId, string $accountId, ?Carbon $fromDate, ?Carbon $toDate)`. Each method calls ConsentEnforcementService first.

PispService: `initiatePayment(string $consentId, array $paymentData)`, `getPaymentStatus(string $paymentId)`. Uses existing `Banking` domain's BankTransferService for actual payment execution.

TppRegistrationService: `register(array $data): TppRegistration`, `update(string $tppId, array $data)`, `deactivate(string $tppId)`, `validateCertificate(string $certificate): bool`.

BerlinGroupAdapter/UkObAdapter: Format transformers that convert internal data structures to standard-specific JSON response formats (field names, pagination, error codes differ between standards).

- [ ] **Step 1-4: Implement services and adapters with tests following TDD**

Each adapter has: `formatAccountList()`, `formatAccountDetail()`, `formatBalances()`, `formatTransactions()`, `formatPaymentResponse()`, `formatConsentResponse()`, `formatErrorResponse()`.

- [ ] **Step 5: Run tests, PHPStan, commit**

```bash
git add app/Domain/OpenBanking/Services/ tests/Unit/Domain/OpenBanking/Services/
git commit -m "feat(openbanking): AISP, PISP, TPP registration, Berlin Group and UK OB adapters"
```

---

## Task 8: Open Banking — Middleware, Routes, GraphQL

**Files:**
- Create: `app/Http/Middleware/ValidateTppCertificate.php`
- Create: `app/Http/Middleware/EnforceConsent.php`
- Create: `app/Domain/OpenBanking/Routes/api.php`
- Create: `graphql/open-banking.graphql`
- Create: `app/GraphQL/Queries/OpenBanking/ConsentQuery.php`
- Create: `app/GraphQL/Mutations/OpenBanking/CreateConsentMutation.php`
- Create: `app/GraphQL/Mutations/OpenBanking/RevokeConsentMutation.php`
- Modify: `graphql/schema.graphql` — add `#import open-banking.graphql`
- Test: `tests/Feature/Api/OpenBanking/ConsentFlowTest.php`

- [ ] **Step 1: Create middleware**

`ValidateTppCertificate`: Extracts client certificate from request header (`X-TPP-Certificate` or `SSL_CLIENT_CERT`), validates against TppRegistration, passes `tpp_id` to request attributes. Returns 403 if invalid and `openbanking.tpp_certificate_validation` is true.

`EnforceConsent`: Extracts `consent_id` from request header (`X-Consent-ID`), calls ConsentEnforcementService to validate access. Returns 403 if consent invalid. Logs access.

- [ ] **Step 2: Create routes**

Routes as specified in the design spec. All routes are prefixed with `v1/open-banking` and protected by `auth:sanctum` and the two new middleware.

- [ ] **Step 3: Create GraphQL schema and resolvers**

Open Banking types: `Consent`, `TppRegistration`, `ConsentPermission` enum.
Queries: `consent(id: ID!)`, `myConsents`.
Mutations: `createConsent(input: CreateConsentInput!)`, `authorizeConsent(id: ID!)`, `revokeConsent(id: ID!)`.

- [ ] **Step 4: Write feature tests**

Integration test covering: TPP registers → creates consent → user authorizes → AISP reads accounts → consent expires. Requires `RefreshDatabase`.

- [ ] **Step 5: Run all tests, PHPStan, php-cs-fixer, commit**

```bash
git add app/Http/Middleware/ValidateTppCertificate.php app/Http/Middleware/EnforceConsent.php app/Domain/OpenBanking/Routes/ graphql/open-banking.graphql graphql/schema.graphql app/GraphQL/Queries/OpenBanking/ app/GraphQL/Mutations/OpenBanking/ tests/Feature/Api/OpenBanking/
git commit -m "feat(openbanking): middleware, REST routes, GraphQL schema, consent flow tests"
```

---

## Task 9: ISO 8583 — Config, Enums, Value Objects

**Files:**
- Create: `config/iso8583.php`
- Create: `app/Domain/ISO8583/Enums/MessageTypeIndicator.php`
- Create: `app/Domain/ISO8583/Enums/ProcessingCode.php`
- Create: `app/Domain/ISO8583/Enums/ResponseCode.php`
- Create: `app/Domain/ISO8583/ValueObjects/Bitmap.php`
- Create: `app/Domain/ISO8583/ValueObjects/DataElement.php`
- Create: `app/Domain/ISO8583/ValueObjects/Iso8583Message.php`
- Test: `tests/Unit/Domain/ISO8583/Enums/MessageTypeIndicatorTest.php`
- Test: `tests/Unit/Domain/ISO8583/ValueObjects/BitmapTest.php`

- [ ] **Step 1: Create config**

```php
<?php
// config/iso8583.php
declare(strict_types=1);

return [
    'enabled' => env('ISO8583_ENABLED', false),
    'version' => env('ISO8583_VERSION', '1993'),
    'header_length' => (int) env('ISO8583_HEADER_LENGTH', 2),
    'encoding' => env('ISO8583_ENCODING', 'ascii'),
    'networks' => [
        'visa' => [
            'host' => env('VISA_ISO8583_HOST'),
            'port' => (int) env('VISA_ISO8583_PORT', 9100),
            'timeout' => (int) env('VISA_ISO8583_TIMEOUT', 30),
            'bin_ranges' => ['4'],
        ],
        'mastercard' => [
            'host' => env('MC_ISO8583_HOST'),
            'port' => (int) env('MC_ISO8583_PORT', 9200),
            'timeout' => (int) env('MC_ISO8583_TIMEOUT', 30),
            'bin_ranges' => ['5', '2'],
        ],
    ],
];
```

- [ ] **Step 2: Create enums**

`MessageTypeIndicator`: 0100 (AUTH_REQUEST), 0110 (AUTH_RESPONSE), 0200 (FINANCIAL_REQUEST), 0210 (FINANCIAL_RESPONSE), 0400 (REVERSAL_REQUEST), 0410 (REVERSAL_RESPONSE), 0500 (SETTLEMENT_REQUEST), 0510 (SETTLEMENT_RESPONSE), 0800 (NETWORK_MANAGEMENT).

`ProcessingCode`: PURCHASE ('00'), CASH_ADVANCE ('01'), REFUND ('20'), BALANCE_INQUIRY ('31').

`ResponseCode`: APPROVED ('00'), DECLINED ('05'), INSUFFICIENT_FUNDS ('51'), EXPIRED_CARD ('54'), INVALID_TRANSACTION ('12'), DO_NOT_HONOR ('57'), SYSTEM_ERROR ('96').

- [ ] **Step 3: Create Bitmap VO**

```php
<?php
// app/Domain/ISO8583/ValueObjects/Bitmap.php
declare(strict_types=1);

namespace App\Domain\ISO8583\ValueObjects;

final class Bitmap
{
    /** @var array<int, bool> field presence flags (1-128) */
    private array $fields = [];

    public function setField(int $fieldNumber): self
    {
        if ($fieldNumber < 1 || $fieldNumber > 128) {
            throw new \InvalidArgumentException("Field number must be 1-128, got {$fieldNumber}");
        }
        $this->fields[$fieldNumber] = true;

        return $this;
    }

    public function hasField(int $fieldNumber): bool
    {
        return $this->fields[$fieldNumber] ?? false;
    }

    public function hasSecondaryBitmap(): bool
    {
        return array_any(array_keys($this->fields), fn (int $f): bool => $f > 64);
    }

    /** @return array<int> */
    public function presentFields(): array
    {
        $fields = array_keys(array_filter($this->fields));
        sort($fields);

        return $fields;
    }

    public function encode(): string
    {
        $bytes = $this->hasSecondaryBitmap() ? 16 : 8;
        $bitmap = str_repeat("\x00", $bytes);

        foreach ($this->fields as $field => $present) {
            if (! $present) {
                continue;
            }
            $byteIndex = (int) (($field - 1) / 8);
            $bitIndex = 7 - (($field - 1) % 8);
            $bitmap[$byteIndex] = chr(ord($bitmap[$byteIndex]) | (1 << $bitIndex));
        }

        // Set bit 1 if secondary bitmap present
        if ($this->hasSecondaryBitmap()) {
            $bitmap[0] = chr(ord($bitmap[0]) | 0x80);
        }

        return bin2hex($bitmap);
    }

    public static function decode(string $hex): self
    {
        $bitmap = new self();
        $bytes = hex2bin($hex);
        if ($bytes === false) {
            throw new \InvalidArgumentException('Invalid hex bitmap');
        }

        $totalBits = strlen($bytes) * 8;
        for ($i = 0; $i < $totalBits; $i++) {
            $byteIndex = (int) ($i / 8);
            $bitIndex = 7 - ($i % 8);
            if (ord($bytes[$byteIndex]) & (1 << $bitIndex)) {
                $bitmap->setField($i + 1);
            }
        }

        return $bitmap;
    }
}
```

- [ ] **Step 4: Create DataElement and Iso8583Message VOs**

`DataElement`: readonly with `fieldNumber`, `value`, `length`, `type` (ALPHA, NUMERIC, LLVAR, LLLVAR).

`Iso8583Message`: contains `MessageTypeIndicator $mti`, `Bitmap $bitmap`, `array<int, DataElement> $dataElements`. Methods: `setField(int $number, string $value)`, `getField(int $number): ?string`, `getMti()`, `getBitmap()`.

- [ ] **Step 5: Write tests**

```php
<?php
// tests/Unit/Domain/ISO8583/ValueObjects/BitmapTest.php
declare(strict_types=1);

use App\Domain\ISO8583\ValueObjects\Bitmap;

it('sets and checks fields', function (): void {
    $bitmap = new Bitmap();
    $bitmap->setField(2)->setField(3)->setField(4);

    expect($bitmap->hasField(2))->toBeTrue();
    expect($bitmap->hasField(3))->toBeTrue();
    expect($bitmap->hasField(5))->toBeFalse();
});

it('encodes primary bitmap to hex', function (): void {
    $bitmap = new Bitmap();
    $bitmap->setField(2)->setField(3)->setField(4);

    $hex = $bitmap->encode();
    expect(strlen($hex))->toBe(16); // 8 bytes = 16 hex chars
});

it('round-trips through encode/decode', function (): void {
    $original = new Bitmap();
    $original->setField(2)->setField(11)->setField(39)->setField(41);

    $decoded = Bitmap::decode($original->encode());
    expect($decoded->presentFields())->toBe($original->presentFields());
});

it('detects secondary bitmap need for fields > 64', function (): void {
    $bitmap = new Bitmap();
    $bitmap->setField(2)->setField(70);

    expect($bitmap->hasSecondaryBitmap())->toBeTrue();
    expect(strlen($bitmap->encode()))->toBe(32); // 16 bytes
});

it('rejects invalid field numbers', function (): void {
    $bitmap = new Bitmap();
    $bitmap->setField(0);
})->throws(\InvalidArgumentException::class);
```

- [ ] **Step 6: Run tests, PHPStan, commit**

```bash
git add config/iso8583.php app/Domain/ISO8583/ tests/Unit/Domain/ISO8583/
git commit -m "feat(iso8583): domain scaffolding — config, enums, bitmap codec, message VOs"
```

---

## Task 10: ISO 8583 — MessageCodec, FieldDefinitions, Handlers

**Files:**
- Create: `app/Domain/ISO8583/Services/FieldDefinitions.php`
- Create: `app/Domain/ISO8583/Services/MessageCodec.php`
- Create: `app/Domain/ISO8583/Services/AuthorizationHandler.php`
- Create: `app/Domain/ISO8583/Services/ReversalHandler.php`
- Create: `app/Domain/ISO8583/Services/SettlementHandler.php`
- Create: `app/Domain/ISO8583/Routes/api.php`
- Test: `tests/Unit/Domain/ISO8583/Services/MessageCodecTest.php`
- Test: `tests/Unit/Domain/ISO8583/Services/AuthorizationHandlerTest.php`

- [ ] **Step 1: Create FieldDefinitions**

Static map of ISO 8583 field numbers to their specs: field number → `['name' => string, 'type' => string, 'max_length' => int]`.

Key fields: 2 (PAN), 3 (Processing Code), 4 (Amount), 7 (Transmission DateTime), 11 (STAN), 12 (Local Time), 13 (Local Date), 22 (POS Entry Mode), 25 (POS Condition Code), 37 (Retrieval Reference), 38 (Authorization Code), 39 (Response Code), 41 (Terminal ID), 42 (Merchant ID), 43 (Merchant Name/Location), 49 (Currency Code), 54 (Additional Amounts).

- [ ] **Step 2: Create MessageCodec**

`encode(Iso8583Message $message): string` — Serialize MTI + bitmap + data elements to binary/hex string.
`decode(string $raw): Iso8583Message` — Parse binary/hex string into Iso8583Message.

Uses FieldDefinitions to know field lengths and types for parsing LLVAR/LLLVAR fields.

- [ ] **Step 3: Create AuthorizationHandler**

`handleRequest(Iso8583Message $request): Iso8583Message` — Process a 0100 authorization request:
1. Extract PAN, amount, currency, merchant info
2. Call `CardIssuance\Services\SpendLimitEnforcementService::checkLimit()` if card found
3. Build 0110 response with appropriate response code
4. Return response message

Uses constructor DI for `SpendLimitEnforcementService`.

- [ ] **Step 4: Create ReversalHandler and SettlementHandler**

`ReversalHandler::handleRequest(Iso8583Message)` — Process 0400 reversal, returns 0410.
`SettlementHandler::handleRequest(Iso8583Message)` — Process 0500 settlement batch, returns 0510.

- [ ] **Step 5: Create routes**

```php
<?php
// app/Domain/ISO8583/Routes/api.php
declare(strict_types=1);

use Illuminate\Http\JsonResponse;
use Illuminate\Http\Request;
use Illuminate\Support\Facades\Route;
use App\Domain\ISO8583\Services\MessageCodec;
use App\Domain\ISO8583\Services\AuthorizationHandler;

Route::prefix('v1/iso8583')->name('api.iso8583.')->middleware(['auth:sanctum'])->group(function (): void {
    Route::post('/authorize', function (Request $request): JsonResponse {
        $request->validate(['message' => ['required', 'string']]);
        $codec = app(MessageCodec::class);
        $handler = app(AuthorizationHandler::class);

        $incoming = $codec->decode($request->input('message'));
        $response = $handler->handleRequest($incoming);

        return response()->json([
            'success' => true,
            'data' => ['response' => $codec->encode($response)],
        ]);
    })->name('authorize');
});
```

- [ ] **Step 6: Write tests**

```php
<?php
// tests/Unit/Domain/ISO8583/Services/MessageCodecTest.php
declare(strict_types=1);

use App\Domain\ISO8583\Enums\MessageTypeIndicator;
use App\Domain\ISO8583\Services\FieldDefinitions;
use App\Domain\ISO8583\Services\MessageCodec;
use App\Domain\ISO8583\ValueObjects\Iso8583Message;

beforeEach(function (): void {
    $this->codec = new MessageCodec(new FieldDefinitions());
});

it('encodes and decodes a simple authorization request', function (): void {
    $msg = new Iso8583Message(MessageTypeIndicator::AUTH_REQUEST);
    $msg->setField(2, '4111111111111111');
    $msg->setField(3, '000000');
    $msg->setField(4, '000000010000'); // $100.00
    $msg->setField(11, '123456');
    $msg->setField(41, 'TERM0001');
    $msg->setField(42, 'MERCH00000001');
    $msg->setField(49, '840');

    $encoded = $this->codec->encode($msg);
    $decoded = $this->codec->decode($encoded);

    expect($decoded->getMti())->toBe(MessageTypeIndicator::AUTH_REQUEST);
    expect($decoded->getField(2))->toBe('4111111111111111');
    expect($decoded->getField(4))->toBe('000000010000');
    expect($decoded->getField(49))->toBe('840');
});

it('handles LLVAR fields correctly', function (): void {
    $msg = new Iso8583Message(MessageTypeIndicator::AUTH_REQUEST);
    $msg->setField(2, '4111111111111111'); // PAN is LLVAR

    $encoded = $this->codec->encode($msg);
    $decoded = $this->codec->decode($encoded);

    expect($decoded->getField(2))->toBe('4111111111111111');
});
```

- [ ] **Step 7: Run all tests, PHPStan, php-cs-fixer**

Run: `XDEBUG_MODE=off vendor/bin/pest tests/Unit/Domain/ISO8583/ tests/Unit/Domain/ISO20022/ tests/Unit/Domain/OpenBanking/`
Run: `XDEBUG_MODE=off vendor/bin/phpstan analyse --memory-limit=2G app/Domain/ISO8583/ app/Domain/ISO20022/ app/Domain/OpenBanking/`
Run: `./vendor/bin/php-cs-fixer fix --config=.php-cs-fixer.php`
Expected: All pass, no errors

- [ ] **Step 8: Commit**

```bash
git add app/Domain/ISO8583/Services/ app/Domain/ISO8583/Routes/ tests/Unit/Domain/ISO8583/
git commit -m "feat(iso8583): message codec, field definitions, auth/reversal/settlement handlers, routes"
```

---

## Task 11: Final Integration — Version Bump, VERSION_ROADMAP, Feature Pages

**Files:**
- Modify: `docs/VERSION_ROADMAP.md` — add v7.2.0 entry
- Modify: `helm/finaegis/Chart.yaml` — bump appVersion
- Run: full test suite, PHPStan, php-cs-fixer

- [ ] **Step 1: Update VERSION_ROADMAP.md**

Add v7.2.0 section documenting:
- ISO 20022 message engine (8 message types, parser/generator/validator)
- Open Banking PSD2 compliance (consent management, AISP/PISP, Berlin Group + UK OB adapters)
- ISO 8583 card network processor (authorization, reversal, settlement handlers)

- [ ] **Step 2: Run full quality checks**

```bash
./vendor/bin/php-cs-fixer fix --config=.php-cs-fixer.php
XDEBUG_MODE=off vendor/bin/phpstan analyse --memory-limit=2G
XDEBUG_MODE=off vendor/bin/pest --parallel --stop-on-failure
```

- [ ] **Step 3: Commit and create PR**

```bash
git add -A
git commit -m "feat: v7.2.0 — Standards & Compliance Foundation (ISO 20022, Open Banking PSD2, ISO 8583)"
```

Create PR targeting main with summary of all 3 new domains.
